[build]
  command = "mkdir -p _site && cp -r assets index.html styles.css script.js _site/"
  publish = "_site"

# Deploy the static site by copying files to a dedicated folder to avoid Vite/Netlify conflicts
